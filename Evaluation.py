
import random

class Evaluation:

	result = []

	def __init__(self, result):
		self.result = resutl

	def tournamentRound(self):
		print (self.result)
	"""
		#may not compare the whole pop?
		for x in range(0,(len(result)/2)):
			
			pick1 = random.randint(0,len(result)) 
			pick2 = random.randint(0,len(result)) 

			if(result[x])


			pass
			"""